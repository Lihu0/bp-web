"use strict";const dig=4;let num_arr=[],bool_arr=[],pgia_arr=[],tries=0,legal=!0,adds=0,rnd_arr=[];const table=document.querySelector("#table"),bp_btns=document.querySelector("#bp_btns");do{let rnd=rnd_num(4);adds=0;do{if(adds++,1e4!=adds){legal=!0,rnd++,rnd%=1e4,rnd_arr=Array.from(rnd.toString()).map(Number);for(let i=0;i<tries;i++){const bool=calcbp(num_arr[i],rnd,4)[0],pgia=calcbp(num_arr[i],rnd,4)[1];if(bool!=bool_arr[i]||pgia!=pgia_arr[i]){legal=!1;break}}for(let i=0;i<rnd_arr.length;i++)for(let j=0;j<rnd_arr.length;j++)if(rnd_arr[i]==rnd_arr[j]&&i!=j){legal=!1;break}4!=rnd_arr.length&&(legal=!1)}}while(!legal&&1e4!=adds);if(1e4!=adds&&1e4!=adds){bp_btns.innerHTML+=`<p>${rnd}</p>`;const bool=prompt(`הניחוש שלי הוא ${rnd} הזן כמות בולים`),pgia=prompt(`הניחוש שלי הוא ${rnd} הזן כמות פגיעות`);tries++,num_arr.push(rnd),bool_arr.push(bool),pgia_arr.push(pgia),table.innerHTML+=`<tr><th>${rnd}</th><th>${bool}</th><th>${pgia}</th><th>${tries}</th></tr>`}}while(4!=bool_arr[bool_arr.length-1]&&1e4!=adds);1e4!=adds?alert(`המחשב הצליח ב ${tries} ניסיונות`):alert("יש לך טעות");